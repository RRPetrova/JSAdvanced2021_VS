function solve(input) {
    let size = +input;
    let res = "";
if (input == null) {
    size = 5;
}
for (let r = 0; r < size; r++) {
    for (let col = 0; col < size; col++) {
        res += "* " 
    
    }
    res += '\n';
    
    
}
return res;
}
console.log(solve(
22
))